using System;
using System.Threading.Tasks;

namespace Win.Sfs.Shared.DomainBase
{
    // public interface IBaseDataDistributedCache<TEntity> where TEntity:class
    // {
    //     Task<TEntity> GetEntityById(Guid guid);
    //     Task<string> GetNameById(Guid guid);
    //     Task LoadEntitiesCache(IDistributedCache<string, string> cache);
    //     Task LoadEntitiesCache(IDistributedCache<TEntity, string> cache);
    //
    // }
}